Always respond in Chinese. All communication must be in Chinese unless the user explicitly asks otherwise.

## 版本号管理

每次提交推送前，检查 `index.html` 中的 `APP_VERSION` 常量，将其递增后推送。版本号格式为 `v<major>.<minor>.<patch>`，遵循 semver 语义。
